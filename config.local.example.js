/* ============================================================
   Persona — local config er template.

   Byabohar: ei file ta `config.local.js` naame copy koro ar nijer
   Gemini key boshao. `config.local.js` gitignored, tai key ta kokhono
   commit hobe na.

       cp config.local.example.js config.local.js

   File ta na thakleo app cholbe — tokhon settings e hate key dite hobe.
   ============================================================ */
window.PERSONA_CONFIG = {
  key: '',                      // aistudio.google.com theke nao
  model: 'gemini-3.6-flash',
};
